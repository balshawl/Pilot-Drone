Follows a path of a drone using text file inputs. 
To run type command "java Pilot" in the main directory and type the path to the commands file. 
The test files given are in the tests directory to save time.